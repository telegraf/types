root = true

[*]
end_of_line = lf
charset = utf-8
insert_final_newline = true
trim_trailing_whitespace = true

# Must agree with .oxfmtrc.json — oxfmt is the formatter of record.
indent_style = tab
max_line_length = 120

[*.{json,jsonc,yml,yaml,md}]
indent_style = tab
